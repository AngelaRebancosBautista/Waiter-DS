# Waiter-DS

This C# program solves the Waiter using stacks and prime numbers.
It repeatedly divides plates numbers into two stacks one divisible by each successive prime and one not for q iterations, then outputs the plates in the order they are removed.
